{% extends 'base.html.twig' %}

{% block title %}Material{% endblock %}

{% block body %}
    <h1>Material</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ material.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ material.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_material_admin_index') }}">back to list</a>

    <a href="{{ path('app_material_admin_edit', {'id': material.id}) }}">edit</a>

    {{ include('material_admin/_delete_form.html.twig') }}
{% endblock %}
